#!/bin/bash

set -e

dotpath="$HOME/code"
dotfiles="https://github.com/KlotzAndrew/dotfiles.git"

mkdir -p "$dotpath"

sudo apt install git

git clone "$dotfiles" "$dotpath"

cd "$dotpath/dotfiles"

./bin/install.sh base

make
